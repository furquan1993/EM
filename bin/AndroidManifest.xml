<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.assassin.expensemanager"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="7"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.assassin.expensemanager.MainActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.assassin.expensemanager.AddExpenseActivity"
            android:label="@string/add_expense_title_text"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name="com.assassin.expensemanager.SummaryActivity"
            android:label="@string/summary_title"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:name="com.assassin.expensemanager.HistoryActivity"
            android:label="@string/history_title"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name="com.assassin.expensemanager.CategoryActivity"
            android:label="@string/category_title"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name="com.assassin.expensemanager.SettingsActivity"
            android:label="@string/settings_title"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>